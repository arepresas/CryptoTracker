logging:
  config: classpath:logback-spring-local.xml
  level:
    org.springframework.web.client.RestTemplate: DEBUG

spring:
  datasource:
    url: jdbc:postgresql://crypto-tracker-db:5432/cryptotracker-db
    username: myuser
    password: mysecretpassword
